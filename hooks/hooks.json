{
  "_deprecated": "interfluence is deprecated in favor of intervoice. The PostToolUse:Edit learn-from-edits hook is intentionally disabled to prevent double-logging when both plugins are installed during migration. The original registration is preserved below for reference.",
  "_original_hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/learn-from-edits.sh",
            "timeout": 5
          }
        ]
      }
    ]
  },
  "hooks": {}
}
